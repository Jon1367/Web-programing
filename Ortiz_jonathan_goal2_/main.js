
function avgNumbers(arr){

    arr = [];
}

var  first = prompt(" Enter first name:");
var last = prompt("Enter last name");

function fullname(first,last){


    var fullname = first + last;

    return fullname;
}

console.log(fullname(first,last));

var word = prompt("Enter a word");

function wordCount(length){
   length = word.length;
   return length;
}

console.log(" your word length is " + wordCount(word));

var array = [2,3,3,5,7];
function charCount(long){

    long = array.length;

    return long;
}

console.log(" your array is " + charCount(array));

function vowelsInWord(){

}